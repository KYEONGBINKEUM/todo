/**
 * Database types (auto-generated from Supabase)
 * For now, we'll use a placeholder type
 * Run `supabase gen types typescript --local` to generate actual types
 */

export type Database = {
  public: {
    Tables: {
      [_ in never]: never;
    };
    Views: {
      [_ in never]: never;
    };
    Functions: {
      [_ in never]: never;
    };
    Enums: {
      [_ in never]: never;
    };
  };
};
